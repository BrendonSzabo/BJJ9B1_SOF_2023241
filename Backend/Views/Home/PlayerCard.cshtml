<style>
    /* Clear floats after columns */
    .playerCard {
        height: 68px;
        width: 100%;
        border: 1px solid #ccc;
        background-color: #f2f2f2;
    }

        .playerCard::after {
            content: "";
            clear: both;
            display: table;
        }

    .playerImgCol {
        display: flex;
        justify-content: center;
        align-items: center;
        float: left;
        width: 68px; /* Each column takes up one-third of the container */
        height: 68px;
        padding: 10px;
        box-sizing: border-box; /* Ensures padding doesn't affect width */
        border: 1px solid #ccc;
        background-color: cyan;
    }

    .playerStatCol {
        display: flex;
        justify-content: center;
        align-items: center;
        float: left;
        width: 200px; /* Each column takes up one-third of the container */
        height: 68px;
        padding: 10px;
        box-sizing: border-box; /* Ensures padding doesn't affect width */
        border: 1px solid #ccc;
        background-color: white;
    }
</style>

<div class="playerCard">
    <div class="playerImgCol">image</div>
    <div class="playerStatCol">
        <div>Name, nationality</div>
        <div>Role price if necessary</div>
    </div>
    <div class="playerImgCol">100</div>
</div>